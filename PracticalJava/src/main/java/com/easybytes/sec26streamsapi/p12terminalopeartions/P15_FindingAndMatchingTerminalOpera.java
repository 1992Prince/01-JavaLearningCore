package com.easybytes.sec26streamsapi.p12terminalopeartions;

public class P15_FindingAndMatchingTerminalOpera {

    /*
    Belows are methods in Stream interface that are used to perform find and match operations.
    All find and match operations are terminal operations.

    1) boolean allMatch(Predicate<? super T> predicate) -
       The allMatch() method in Java's Stream API is a terminal operation that checks whether all elements of the stream match a given predicate.
       It returns a boolean value: true if all elements satisfy the predicate, and false otherwise
    2) boolean anyMatch(Predicate<? super T> predicate)
    3) boolean noneMatch(Predicate<? super T> predicate)
    4) Optional<T> findAny()
    5) Optional<T> findFirst()
     */
}
