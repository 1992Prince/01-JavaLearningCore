package com.easybytes.sec26streamsapi.tim.p3terminalop;

public class P1_Intro {

    /**
     * 🧠 What are Terminal Operations?
     * Terminal operations are the final step in the stream pipeline.
     * Once a terminal op is called, the stream is consumed — you can’t reuse it.
     *
     * ✅ Categories of Terminal Operations:
     * 🔹 1. Collecting Result → collect(), toList(), toSet(), etc.
     * 🔹 2. ForEach / Side Effect → forEach(), forEachOrdered()
     * 🔹 3. Aggregation → count(), sum(), average(), min(), max()
     * 🔹 4. Matching / Searching → anyMatch(), allMatch(), noneMatch(), findFirst(), findAny()
     * 🔹 5. Reducing → reduce()
     */
}
